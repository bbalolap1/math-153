# Question Generation

The milestone uses bounded, deterministic templates—not unrestricted SymPy generation. A seed controls manageable coefficients; each problem records construction ID, source/concept references, method, seed, skills, layer, and a reason for the variation. SymPy validates answers only. Current templates are scaffolds pending course-source review.
