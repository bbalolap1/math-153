# Journey: Next-Line Coach

**Status:** needs owner decision; not implemented; no wireframe in this approval round.

```text
Entry
→ choose reviewed family/path
→ PROBLEM_PRESENTATION
→ PAPER_WORK: write only the next line on paper
→ enter one line
→ StepEngine validates against accepted branch graph (proposed—not implemented)
→ valid: advance mathematical state without revealing future steps
→ invalid: bounded line-specific clue
→ retry escalation: clue → rule card → partial example → explanation
→ complete branch
→ SESSION_COMPLETE with rule/error summary
```

Owner decisions required: typed-line format, retry limits, optional domain statements, hint escalation, branch visualization, and whether a full solution is ever shown.
